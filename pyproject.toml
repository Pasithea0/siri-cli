[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "siri-cli"
version = "1.1.0"
description = "Ask the real macOS Siri from the terminal and read back its response."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "admin" }]
keywords = ["siri", "macos", "accessibility", "mcp", "agent", "automation"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: MacOS",
    "Topic :: System :: Shells",
]
dependencies = [
    "pyobjc-framework-ApplicationServices>=9.0",
    "pyobjc-framework-Vision>=9.0",
    "click>=8.0",
    "mcp>=1.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-mock>=3.0",
    "mcp[cli]",
]

[project.scripts]
siri = "siri_cli.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
